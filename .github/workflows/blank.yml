name: CI
on:
  push:
    branches: [ task2 ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build -t task2 .
      - name: Test
        run: docker run --rm task2 pytest main_test.py
