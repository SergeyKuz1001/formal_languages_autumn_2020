name: CI
on:
  push:
    branches: [ task1 ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: docker build -t task1 .
      - name: Test
        run: docker run --rm task1 pytest tests.py
