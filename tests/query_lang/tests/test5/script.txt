' example of correct program # 5 `

connect to
    "data_bases/";

S  -> "a" | CB;
CB -> "c"."b";

select
    edges
from
    graph "data_base_1.txt" intersect with
    query CB;

select
    edges
from
    graph "data_base_1.txt" intersect with
    query S;

select
    count of edges
from
    graph "data_base_1.txt" intersect with
    query S
where each edge is labeled as CB;
